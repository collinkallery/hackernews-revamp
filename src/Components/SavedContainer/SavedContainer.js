import React from "react";
import styled from "styled-components";
import {darkTheme} from "../../theme/globalStyle";

const {
  primaryPurple,
  primaryBlue,
  secondaryTeal,
  background,
  textColor,
  error
} = darkTheme;

const SavedContainer = () => {
  return (
    <p>SavedContainer Mounted</p>
  )
}

export default SavedContainer;
